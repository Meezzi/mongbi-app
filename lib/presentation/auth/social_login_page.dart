import 'package:flutter/material.dart'; // 이걸로 수정

class SocialLoginPage extends StatelessWidget {
  const SocialLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center());
  }
}
